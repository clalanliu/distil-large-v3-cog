build:
  gpu: true
  system_packages:
    - ffmpeg
  python_version: "3.11"
  python_packages:
    - "transformers"
    - "torch"
    - "accelerate"
    - datasets[audio]
    - torchaudio
predict: "predict.py:Predictor"
